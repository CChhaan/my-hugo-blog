---
# 文章标题
title: 1.1 入门指南
# 文章内容摘要
# description:
# 文章内容关键字
keywords: Vue I18n, Vue 国际化, Vue 多语言, createI18n, Vue i18n 教程, Vue $t 方法, Vue 本地化, Vue 语言切换, fallbackLocale, i18n 插件注册, Vue i18n 配置, useI18n, i18n-t 组件, Vue 国际化插件, Vue 国际化实例创建
# 发表日期
date: 2025-05-10
summary: 本文介绍了 Vue I18n 的入门指南，包括如何创建 i18n 实例、注册 i18n 插件以及如何在模板中使用 $t 翻译 API 进行本地化。
# 分类
categories:
  - Vue I18n
# 标签
tags:
  - vue
  - i18n
  - 前端开发
---

```vue
<template>
  <h1>{{ $t("message.hello") }}</h1>
</template>
```

在模板中，使用 Vue I18n 注入的 $t 翻译 API 进行本地化。

这允许 Vue I18n 在不重写模板的情况下更改语言环境，同时能够支持全局应用程序。

## 创建 i18n 实例

通过调用 createI18n 函数创建 i18n 实例。

可以为 createI18n 指定以下重要选项：

- **locale**：要本地化的 Vue 应用的语言。
- **fallbackLocale**：如果 `$t` 翻译 API 中指定的键资源在当前语言中找不到，则回退到的语言。
- **messages**：使用 `$t` 翻译 API 进行翻译的本地化消息。本地化消息的结构是**层次化的对象结构**，每个本地化语言作为顶层属性。

```js
const i18n = createI18n({
  locale: "ja",
  fallbackLocale: "en",
  messages: {
    en: {
      message: {
        hello: "hello world",
      },
    },
    ja: {
      message: {
        hello: "こんにちは、世界",
      },
    },
  },
});
```

## 注册 i18n 插件

创建好 i18n 实例后，需要通过 use 方法在应用上注册插件：

```js
const app = createApp(Vue);
app.use(i18n);
app.mount("#app");
```

与大多数 Vue 插件一样，调用 use 必须在调用 mount 之前。

插件的职责：

- 添加全局属性和方法，如 **$t**、**$i18n**
- 启用 **useI18n** 组合式 API
- 全局注册 **i18n-t**、**i18n-d** 和 **i18n-n** 组件
